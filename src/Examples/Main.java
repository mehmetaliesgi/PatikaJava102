package Examples;

import java.math.BigDecimal;
import java.text.DecimalFormat;

public class Main {
    public static void main(String[] args) {
        /*SortCharactersByFrequency sortCharacter = new SortCharactersByFrequency();
        System.out.println(sortCharacter.shortCharactersByFrequency("Kartalll"));
*/
        /*CreateFixture cFixture = new CreateFixture();
        cFixture.createFixture(cFixture.createTeams());
        */

        /*Calculate<Double> calculate = new Calculate<>(6.6676,2.1122);
        double result = calculate.getValue1()/calculate.getValue2();
        System.out.println(result);

        DecimalFormat df = new DecimalFormat("###.###");
        System.out.println(df.format(result));*/
    }

}
